after install docker 

sudo groupadd docker
sudo gpasswd -a $USER docker
newgrp docker

run 
sudo apt-get install docker-compose


create yml file with name docker-compose-grafana.yml
#######################
version: '3'

services:
  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    hostname: grafana
    user: 'root'
    volumes:
      - grafana_data:/var/lib/grafana
      - grafana_config:/etc/grafana
    ports:
      - 3000:3000
    restart: unless-stopped
    networks:
      - monitoring

networks:
  monitoring:
    external: true

volumes:
  grafana_data:
  grafana_config:
#######################
 
docker-compose -f docker-compose-grafana.yml up -d


create yml file with name docker-compose-prometheus.yml

#######################
version: '3'

services:
  prometheus:
    image: bitnami/prometheus:latest
    container_name: prometheus
    hostname: prometheus
    volumes:
      - prometheus_config:/etc/prometheus
      - prometheus_monitoring_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/etc/prometheus/console_libraries'
      - '--web.console.templates=/etc/prometheus/consoles'
      - '--storage.tsdb.retention.time=30d'
      - '--web.enable-lifecycle'
    ports:
      - 9090:9090
    restart: unless-stopped
    networks:
      - monitoring

networks:
  monitoring:
    external: true

volumes:
  prometheus_config:
  prometheus_monitoring_data:

#######################

docker-compose -f docker-compose-prometheus.yml up -d

##################
                                                                                                                     
version: "3"

networks:
  monitoring:
    external: true

services:
  loki:
    image: grafana/loki:latest
    container_name: loki
    hostname: loki
    ports:
      - "3100:3100"
    command: -config.file=/etc/loki/local-config.yaml
    networks:
      - monitoring
######################
docker-compose -f docker-compose-loki.yml up -d 
################
List all containers (only IDs)=> docker ps -aq;
Stop all running containers. =>docker stop $(docker ps -aq);
Remove all containers.=> docker rm $(docker ps -aq);
Remove all images. =>docker rmi $(docker images -q);

 


help
https://signoz.io/guides/how-to-install-prometheus-and-grafana-on-docker/
https://www.youtube.com/watch?v=vprZf9aylr4



